export function PreDraftedActions() {
  return (
    <div>
      <h2 className="text-xs uppercase tracking-[0.18em] text-text-muted">
        Pre-drafted actions
      </h2>
      <p className="mt-6 max-w-prose text-[15px] leading-relaxed text-text-muted">
        Drafts appear here after the morning brief runs. Approve with one
        keystroke — execution writes through <span className="text-accent">salesforce_crm</span>.
      </p>
    </div>
  );
}
