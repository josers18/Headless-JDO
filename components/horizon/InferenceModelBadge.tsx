"use client";

import type { InferenceMeta } from "@/lib/client/useAgentStream";

/** Single Claude primary stack — badge hidden for cleaner film/demo UI. */
export function InferenceModelBadge(_props: {
  meta: InferenceMeta | null;
  className?: string;
}) {
  return null;
}
